export "gen/token_kinds"

File :: struct {
	path: string,
	contents: string,
	lines: [dynamic]string,
	tokens: [dynamic]Token,
	//ast: Ast_Node
}

Token :: struct {
	kind: Token_Kind,
	text: string,
	file: ^File,
	line, col: u32,
}